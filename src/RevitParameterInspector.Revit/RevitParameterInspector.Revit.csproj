<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- net48 => Revit 2024, net8.0-windows => Revit 2025 (default) or 2026 via -p:RevitVersion=2026 -->
    <TargetFrameworks>net48;net8.0-windows</TargetFrameworks>
    <RootNamespace>RevitParameterInspector.Revit</RootNamespace>
    <AssemblyName>RevitParameterInspector.Revit</AssemblyName>
    <GenerateDependencyFile>false</GenerateDependencyFile>
    <!-- Needed for RevitAPIUI's ribbon image properties (System.Windows.Media.ImageSource/BitmapImage). -->
    <UseWPF>true</UseWPF>
    <!--
      This is a Revit plug-in, not an app: nothing runs "dotnet RevitParameterInspector.Revit.dll"
      to resolve a deps.json, Revit just loads the dll from a folder. .NET Framework (net48)
      always copies package-reference assemblies next to the output; .NET (net8.0-windows)
      class libraries do not by default (they expect a deps.json instead, which we disable
      above), so without this, transitive package assemblies (e.g. Export's ClosedXML) were
      silently missing from the net8.0-windows output (Revit 2025/2026).
    -->
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
  </PropertyGroup>

  <!-- Resolve which Revit version this TFM+property combination targets. -->
  <PropertyGroup Condition="'$(TargetFramework)'=='net48'">
    <EffectiveRevitVersion>2024</EffectiveRevitVersion>
  </PropertyGroup>
  <PropertyGroup Condition="'$(TargetFramework)'=='net8.0-windows' AND '$(RevitVersion)'==''">
    <EffectiveRevitVersion>2025</EffectiveRevitVersion>
  </PropertyGroup>
  <PropertyGroup Condition="'$(TargetFramework)'=='net8.0-windows' AND '$(RevitVersion)'!=''">
    <EffectiveRevitVersion>$(RevitVersion)</EffectiveRevitVersion>
  </PropertyGroup>

  <PropertyGroup Condition="'$(EffectiveRevitVersion)'=='2024'">
    <DefineConstants>$(DefineConstants);REVIT2024;REVIT2024_OR_GREATER</DefineConstants>
  </PropertyGroup>
  <PropertyGroup Condition="'$(EffectiveRevitVersion)'=='2025'">
    <DefineConstants>$(DefineConstants);REVIT2025;REVIT2024_OR_GREATER;REVIT2025_OR_GREATER</DefineConstants>
  </PropertyGroup>
  <PropertyGroup Condition="'$(EffectiveRevitVersion)'=='2026'">
    <DefineConstants>$(DefineConstants);REVIT2026;REVIT2024_OR_GREATER;REVIT2025_OR_GREATER</DefineConstants>
  </PropertyGroup>

  <!-- Override with an environment variable or -p:RevitInstallDir=... if Revit is installed elsewhere. -->
  <PropertyGroup Condition="'$(RevitInstallDir)'==''">
    <RevitInstallDir>C:\Program Files\Autodesk\Revit $(EffectiveRevitVersion)</RevitInstallDir>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="RevitAPI">
      <HintPath>$(RevitInstallDir)\RevitAPI.dll</HintPath>
      <Private>false</Private>
      <SpecificVersion>false</SpecificVersion>
    </Reference>
    <Reference Include="RevitAPIUI">
      <HintPath>$(RevitInstallDir)\RevitAPIUI.dll</HintPath>
      <Private>false</Private>
      <SpecificVersion>false</SpecificVersion>
    </Reference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RevitParameterInspector.Core\RevitParameterInspector.Core.csproj" />
    <ProjectReference Include="..\RevitParameterInspector.UI\RevitParameterInspector.UI.csproj" />
    <ProjectReference Include="..\RevitParameterInspector.Dictionary\RevitParameterInspector.Dictionary.csproj" />
  </ItemGroup>

  <!-- Ship the built-in dictionary next to the add-in so DictionaryEngine can find it at runtime. -->
  <ItemGroup>
    <None Include="..\..\dictionary\**\*.json">
      <Link>dictionary\%(RecursiveDir)%(Filename)%(Extension)</Link>
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <!-- Ship the ribbon button icons next to the add-in so RevitParameterInspectorApplication can find them at runtime. -->
  <ItemGroup>
    <None Include="..\..\ICON\*.png">
      <Link>icons\%(Filename)%(Extension)</Link>
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
